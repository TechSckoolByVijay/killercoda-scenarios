# Step 3 — Paths & Navigation Mastery 🗺️

## Absolute Paths (Full Address)

```bash
cd /tmp/demo/bin && pwd
```{{exec}}

**Absolute = complete address** from root (/)

---

## Relative Paths (From Here)

```bash
cd ../etc && pwd
```{{exec}}

**Relative = directions** from current location

---

## Advanced Navigation

```bash
cd ~/workspace/projects/mobile-app && pwd
```{{exec}}

```bash
cd ../../documents && pwd  
```{{exec}}

**Jumped multiple levels** with `../..`

---

## Master Challenge

**Navigate like a pro:** Root → Home → Project → Back to docs

```bash
cd / && cd ~ && cd workspace/projects && cd ../documents && pwd
```{{exec}}

---

## 🎯 Path Types

**Absolute:** `/home/user/workspace` (full address)  
**Relative:** `../projects/web-app` (from current spot)

## 💡 Pro Navigation Tips

- **Tab completion** saves typing
- **`pwd` when lost**
- **Use `..` for quick backtracking**
- **Combine with `&&`** for multi-step navigation

**Navigation mastery complete!** 🏆